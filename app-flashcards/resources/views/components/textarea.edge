<label for="{{name}}">{{label}} :</label>
<textarea name="{{name}}" id="{{name}}" placehold="{{label}}">{{value}}</textarea>
@inputError(name)
@each(message in $messages)
<p class="text-danger">{{message}}</p>
@end
@end